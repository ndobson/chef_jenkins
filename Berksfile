source 'https://supermarket.chef.io'

metadata

group :integration do
  cookbook 'java'
  cookbook 'jenkins_job', path: 'test/fixtures/cookbooks/jenkins_job'
  cookbook 'jenkins_server_wrapper', path: 'test/fixtures/cookbooks/jenkins_server_wrapper'
end
